import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.my.componenttask',
  appName: 'Component Task',
  webDir: 'web/dist'
};

export default config;
